class CategoryModel {
  constructor({ id, name, color, emoji, order }) {
    this.id = id;
    this.name = name;
    this.color = color;
    this.emoji = emoji;
    this.order = order;
  }
}

export default CategoryModel;
